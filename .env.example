PORT=3000
SESSION_SECRET=change-this-to-a-long-random-string
NODE_ENV=development

# Optional: powers the "Ask AI" widget on student dashboards. Without this
# set, the widget still appears but replies with a message explaining it
# isn't configured yet. Get a key at https://console.anthropic.com/
ANTHROPIC_API_KEY=
